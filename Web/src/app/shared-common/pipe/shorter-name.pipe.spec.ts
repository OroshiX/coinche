import { ShorterNamePipe } from './shorter-name.pipe';

describe('ShorterNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShorterNamePipe();
    expect(pipe).toBeTruthy();
  });
});
